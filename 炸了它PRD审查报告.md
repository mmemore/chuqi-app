# 《炸了它》PRD V2.0 审查报告

**审查对象**：`炸了它产品需求文档V2.0.md`
**审查日期**：2026-09-07
**审查视角**：即将立项开发（当前代码为空模板，全部功能待开发）
**审查人**：产品经理（审查报告，不改动原文）

---

## 一、总评

**结论：这份 PRD 作为「产品愿景说明」已经合格，但作为「可立项开发的 PRD」尚未达标——它说清了「做什么」，却没定义「做完的标准」和「能不能做出来的前提」。**

当前最致命的三个问题，按严重程度排序：

1. **后端需求完全缺失**：小程序码、内容安全（敏感词）、语音处理、数据统计全部需要服务端/云函数，PRD 只写了前端。缺了这层，P0 的海报分享和合规要求都无法落地。
2. **没有验收标准**：所有功能只有「规格说明」，没有 Given-When-Then 验收条件，研发无法判断「做完」，测试无法写用例。
3. **成功指标没有基线、目标值、测量窗口**：§7.1 列了指标却只有「说明/用途」，无法回答「上线后做到什么算成功」。

置信度约 85%。理由：以上缺口均可通过 PRD 原文与标准 PRD 模板逐条对照确认；仅「后端是否已有外部供应商」这一项无法从文档判断，已列入待确认清单。

---

## 二、结构性缺失（对照标准 PRD 模板）

| 编号 | 缺失章节 | 现状 | 为什么是问题 | 建议 |
|------|---------|------|-------------|------|
| S1 | 问题陈述 + 证据 | §1.1 只有一句定性背景 | 无法证明「这个产品值得做」，也说不清不做会怎样 | 补一节：目标用户的痛点、频率、不做的代价，并附访谈（n=？）、竞品行为数据 |
| S2 | 成功指标的基线/目标值/测量窗口 | §7.1 只有说明与用途 | 无法衡量成败 | 每个指标补 3 列：当前基线、目标值、测量窗口（如「爆炸触发率 ≥ 60%，上线后 30 天」） |
| S3 | 非目标 + 重启条件 | 仅 MoSCoW 表格 | 没有「为什么不做」，后续容易范围蔓延 | 把 P2/P3 逐条写清「延后原因 + 什么信号下重启」 |
| S4 | 验收标准 | 每个功能只有规格说明 | 研发无法判「完成」 | 每个 P0/P1 功能补 3-5 条 Given-When-Then 验收项 |
| S5 | 发布计划（通过门槛/受众/回滚标准） | §8 里程碑只有阶段和目标 | 无法判断「这个阶段算过没过」 | 给 Phase 0-4 各补「通过门槛」与「回滚标准」 |
| S6 | 版本变更记录 | V2.0 无 changelog | 无法追踪 V1.0→V2.0 改了什么、为什么 | 补一节 changelog |
| S7 | 待确认清单 + 下一步动作 | 全文无 | 悬而未决的假设散落各处 | 见本报告第八、九节 |

---

## 三、需求不明确清单（用户重点关注，逐功能）

> 每条格式：**位置 → 问题 → 建议写法**。这是研发/测试拿到手最卡壳的地方。

| 编号 | 位置 | 需求原文 | 不明确之处 | 建议写法 |
|------|------|---------|-----------|---------|
| R1 | §3.2.1 | 点击频率越高，怒气值增长越快 | 点击一次 +多少？频率与增速的映射无公式 | 定义规则，如「单次点击 +2%，每秒最多计 10 次有效点击，超出按 10 次封顶」 |
| R2 | §3.2.1 | 屏幕边缘随机弹出拟声词 | 弹出频率？同屏上限？存活时长？位置范围？ | 如「每 0.8-1.5 秒随机 1 个，同屏最多 4 个，单个存活 1 秒，落在屏幕左右各 20% 边缘区」 |
| R3 | §3.2.1 | 爆炸 1.5 秒后自动重置 | 重置后怒气值是否归零？粒子数量？分辨率适配？ | 明确「重置=怒气值归 0，回到初始沙袋状态」；粒子数量给基准（如标准 200 个/低端机 60 个） |
| R4 | §3.2.2 | 爆炸特效结束后自动弹出海报 | 可否跳过/关闭？用户拒绝相册授权怎么办？ | 补「右上角可关闭，不阻断继续使用」+「授权被拒时降级为仅显示、不保存」 |
| R5 | §3.2.1 | 手机短促震动 vibrateShort() | 固定 15ms，未说明连点节流、强度分级 | 补「单次震动 15ms，200ms 内不重复触发；设置页可切长震/短震」 |
| R6 | §3.4.1 | 变声（外星人/小黄人/机器人）+ Web Audio API 或第三方 SDK | 变声效果无验收标准；技术选型未决 | 定一个方案，并给验收项（如「录音 3s，变声后音高 ±X%，播放 2 遍循环」） |
| R7 | §3.3.1 | 每个对象被点击时形变效果略有差异（如老板会脸红） | 「略有差异」无统一规范 | 列一张对象×形变对照表，明确每种对象的差异化表现 |
| R8 | §3.3.2 | 清除缓存：清除本地存储的统计数据 | 「缓存」范围不清 | 明确清哪些（仅统计计数？还是含设置项？）并标注「设置项保留/重置」 |
| R9 | §3.2.1 | 出气对象默认 Q 版沙袋（带表情） | 表情有几帧？形变幅度多少？ | 补「表情 3 帧（平静/受力/爆裂前）」，形变挤压幅度给数值（如 scaleX 0.7） |
| R10 | §5.1 | 所有反馈必须在 100ms 内出现 | 与 §4「点击响应 <50ms」是两个数，口径不清 | 统一为一个可测指标，并说明测量方式 |

---

## 四、内部矛盾清单

| 编号 | 矛盾双方 | 冲突内容 | 需要决策 |
|------|---------|---------|---------|
| C1 | §3.3.2 vs §9 | 震动默认值：设置页说「默认开启」，风险应对说「默认关闭+首次弹窗询问」 | 二选一，建议「默认开启 + 设置页可关」，因为「默认关」会削弱核心强反馈卖点 |
| C2 | §5.2 vs §3.1 | 首页结构含「今日情绪报告入口」（P2 功能），但 §3.1 把情绪报告定为 P2 | 若 P2 排期在 V1.1，首页入口应 V1.1 再加，V1.0 首页不放 |
| C3 | §4 vs §6.1 | 包体积 ≤1.5MB vs 建议引入 pixi.js（体积大） | pixi.js 会挤压 1.5MB 预算，需决策：用轻量 Canvas 自研粒子，还是放宽包体积 |
| C4 | §6.1 | 粒子特效「Canvas 2D 或 pixi.js」、语音「Web Audio API 或第三方 SDK」 | 需求文档不该用「或」悬空，立项前需敲定唯一方案 |

---

## 五、落地阻塞项（「写不了/做不了」的缺口）

| 编号 | 缺口 | 影响 | 建议 |
|------|------|------|------|
| B1 | **后端/云函数缺失** | 小程序码生成、内容安全 API、语音处理、数据统计均无服务端方案，P0 海报分享与合规无法落地 | 补「后端需求」章节：接口清单、云函数/自建服务选型、负责人 |
| B2 | 埋点事件清单缺失 | §7.1 有指标但无事件名/触发时机/参数，研发无法开发埋点 | 补埋点表：事件名（如 `anger_explode`）、触发时机、上报字段 |
| B3 | 隐私合规缺失 | 收集统计数据需隐私政策/用户协议，未提埋点是否需授权 | 补「隐私与合规」小节，明确统计数据的授权与告知方式 |
| B4 | 商业模式空白 | P3 广告「V1.5 再考虑」，整份 PRD 无北极星指标与变现路径 | 明确北极星指标（建议「日均爆炸完成次数」），并给商业化时间表 |
| B5 | 竞品分析太浅 | §10.1 每个竞品仅一句话，无功能对比表、无数据 | 补竞品对比表 + 我们差异化的验证方式 |
| B6 | 用户画像与功能断裂 | 4 个画像无任何差异化功能设计 | 要么让画像反哺功能（如「社恐青年」驱动匿名设计），要么精简画像 |

---

## 六、PRD 与实际实现的差距

> 经只读核对，当前仓库是**未经定制的 unibest 空模板**，PRD 所述全部功能均未实现：

| 功能 | 实现状态 |
|------|---------|
| 点击出气核心交互（怒气值/震动/粒子） | 未实现 |
| 情绪海报生成与分享 | 未实现 |
| 多出气对象切换 | 未实现 |
| 设置页（震动/音效/清缓存） | 未实现 |
| 语音变声出气 | 未实现 |
| 每日情绪报告/统计 | 未实现 |

- 页面仅 3 个模板占位页，无任何自定义组件；`angerStore.ts`、`PunchBag.vue` 等 PRD 规划文件均不存在。
- 无后端/云函数；`manifest.json` 中 `uniStatistics.enable=false`，无微信分享/内容安全插件。
- 依赖中无 `pixi.js`、无音频处理库、无内容安全 SDK。

**含义**：这份 PRD 是「立项前」文档，上面的执行级缺口（验收标准、后端、埋点）不是可选项，而是开发启动前的必要条件。

---

## 七、优先级排序（建议按此顺序补）

| 优先级 | 事项 | 理由 |
|--------|------|------|
| P0（立项前必须） | 补后端需求（B1）+ 敲定内部矛盾 C1/C3/C4 | 不做则 P0 功能无法真正落地 |
| P0（立项前必须） | 补成功指标基线/目标值/窗口（S2） | 无指标无法立项、无法验收 |
| P0（开发前必须） | 为 P0 功能补验收标准（S4 + R1-R5、R9） | 研发测试无法开工 |
| P1（开发前） | 补埋点事件清单（B2）+ 隐私合规（B3） | 埋点与合规要随功能一起做，否则返工 |
| P1（开发前） | 补问题陈述与证据（S1）+ 竞品对比（B5） | 支撑立项说服力，但不阻塞开发 |
| P2（可延后） | 商业模式与北极星指标（B4）、画像反哺（B6） | V1.0 可不阻塞，V1.1 前需明确 |

---

## 八、待确认清单

| 编号 | 需确认内容 | 影响章节 | 找谁确认 |
|------|-----------|---------|---------|
| 1 | 后端是否有现成服务/云函数，还是需从零搭建 | 后端需求、§6 技术方案 | 研发负责人 |
| 2 | 小程序码用微信官方 `getUnlimited` 还是第三方生成 | §3.2.2 海报 | 研发负责人 |
| 3 | 震动默认值最终定「默认开」还是「默认关+首次询问」 | §3.3.2 / §9 矛盾 C1 | 产品 + 运营 |
| 4 | 粒子特效选 Canvas 自研还是 pixi.js（受 1.5MB 包体积约束） | §6.1 矛盾 C3 | 前端负责人 |
| 5 | 各成功指标的目标值（如爆炸触发率目标 60%？分享率目标 15%？） | §7.1 | 产品负责人 |
| 6 | 是否已做用户访谈/竞品试用，数据基线是多少 | §1 问题陈述 | 产品负责人 |
| 7 | 统计数据的隐私政策/用户授权方案 | §7、隐私合规 | 法务 + 产品 |

---

## 九、下一步动作

| 序号 | 动作 | 负责人 | 截止时间 |
|------|------|--------|---------|
| 1 | 逐条确认「待确认清单」第 1-7 项 | 产品负责人 | 立项前 |
| 2 | 补后端需求章节（接口清单 + 选型） | 研发负责人 | 立项评审会前 |
| 3 | 敲定内部矛盾 C1/C3/C4 的唯一方案 | 产品 + 前端 | 立项评审会前 |
| 4 | 为 P0 功能补 Given-When-Then 验收标准 | 产品负责人 | 开发启动前 |
| 5 | 补成功指标基线/目标值/测量窗口 | 产品负责人 | 立项评审会前 |
| 6 | 补埋点事件清单 + 隐私合规方案 | 产品 + 研发 | 开发启动前 |
